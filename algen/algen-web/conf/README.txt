CONFIGURATION

* Spark executables and configurations can be found here, as sample Windows cmd to:
- launch Master, Workers, Server History from <SPARK_HOME>/bin on a Spark Standalone Cluster
- submit applications from <PROJECT_HOME>

* Spark example configurations are provided:
- log4j
- spark-defaults

* For other Spark REST Client parameters, please look an example at SparkTaskExecutor:runDistributed() method
